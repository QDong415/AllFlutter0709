---
description: Flutter 编码规范（Model 命名、Widget key、中文注释）
globs: "**/*.dart"
alwaysApply: false
---

# Flutter Coding Rules

手写 Dart 代码必须遵守本规范。`*.g.dart`、`*.freezed.dart` 等生成文件除外。

## 1. Model 命名

Model 类型变量名必须以 `Model` 结尾。

```dart
// ✅
final UserModel userModel;
final TopicModel topicModel;

// ❌
final UserModel user;
final TopicModel topic;
```

## 2. Widget 构造方法

`key`（含 `super.key`）必须放在构造参数第一位。

```dart
// ✅
const UserAvatarWidget({
  super.key,
  required this.userModel,
});

// ❌
const UserAvatarWidget({
  required this.userModel,
  super.key,
});
```

## 3. Class 注释

手写 Class 必须加中文文档注释，说明职责；禁止无意义注释。

```dart
/// 用户头像展示组件
class UserAvatarWidget {}
```

例外：极短 private 内部类、生成代码中的 Class。

## 4. Public 方法注释

手写 public 方法 / getter / setter 必须加中文文档注释。private 方法不强制。

```dart
/// 加载用户详情数据
Future<void> loadUserDetail(String userId) async {}
```

例外：一目了然的简单 getter/setter、纯 `override`（如 `build` / `dispose`）、接口转发实现。

## 5. 界面文件拆分

新增或大改界面时，不要把整页堆在一个 dart 文件里。按下面拆，直接执行，不必再去翻其他旧页面源码。

目录约定（以某功能 `foo` 为例）：

```text
presentation/
  foo_page.dart                 # 主文件：只编排状态与组装
  widgets/
    foo_header.dart             # 独立 UI 块
    foo_input_bar.dart
  helpers/
    foo_send_helper.dart        # 不依赖 Widget 树的业务逻辑
    foo_list_local_helper.dart
```

怎么拆：
- 能单独成块的 UI → `presentation/widgets/`
- 发送、列表本地更新、滚动等逻辑 → `presentation/helpers/`
- 主文件只做协调，尽量短

例外：300行内、没有明显独立子块的短页面可不拆。
